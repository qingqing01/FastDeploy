# 支持模型列表

FastDeploy目前支持模型列表如下，注意在加载模型或启动服务时，指定```model```参数为如下模型名，且当前路径不存在相应目录时，FastDeploy会自自动下载模型。

| 模型名 | 支持无损量化方式 | 硬件要求说明 |
| :----- | :--------------  | :----------- |
| Qwen2-7B-Instruct | xxxx 待填写 | A/H卡，最小显存24GB |

